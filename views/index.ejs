<!DOCTYPE html>
<html lang="es">

<head>
    <%- include('partials/head', { title: 'Comedor de los Pobres' ,
        description: 'Asociación Juan XXIII - Comedor de los Pobres P. Roberto Infante en Nuevo León. Transformando vidas a través de la nutrición digna.'
        }) %>
        <style>
            .blob {
                position: absolute;
                width: 500px;
                height: 500px;
                background: linear-gradient(135deg, rgba(124, 58, 237, 0.2) 0%, rgba(249, 115, 22, 0.2) 100%);
                filter: blur(80px);
                border-radius: 50%;
                z-index: -1;
            }

            .btn-glow:hover {
                box-shadow: 0 0 20px rgba(249, 115, 22, 0.4);
            }

            /* ----- Carousel Slide Transitions ----- */
            .slide-3d {
                position: absolute;
                inset: 0;
                width: 100%;
                height: 100%;
                transition: transform 0.7s cubic-bezier(0.4, 0, 0.2, 1), opacity 0.7s cubic-bezier(0.4, 0, 0.2, 1);
            }

            .slide-3d-active {
                transform: translateX(0) scale(1);
                opacity: 1;
                z-index: 10;
                pointer-events: auto;
            }

            .slide-3d-right {
                transform: translateX(100%) scale(0.96);
                opacity: 0;
                z-index: 0;
                pointer-events: none;
            }

            .slide-3d-left {
                transform: translateX(-100%) scale(0.96);
                opacity: 0;
                z-index: 0;
                pointer-events: none;
            }
        </style>
</head>

<body class="overflow-x-hidden">

    <!-- NAVBAR -->
    <%- include('partials/navbar') %>

        <!-- Hero Section -->
        <section class="relative min-h-[90dvh] md:min-h-[90vh] flex items-center overflow-hidden py-12 sm:py-16 lg:py-0">
            <!-- Elementos decorativos de fondo -->
            <div class="blob -top-20 -right-20" style="background: rgba(251, 191, 36, 0.15);"></div>
            <div class="blob -bottom-20 -left-20" style="background: rgba(124, 58, 237, 0.15);"></div>

            <div
                class="max-w-7xl mx-auto px-4 sm:px-6 grid grid-cols-1 lg:grid-cols-12 gap-8 sm:gap-10 lg:gap-12 items-center">

                <!-- Contenido Texto -->
                <div class="lg:col-span-7 z-10">
                    <div
                        class="inline-flex items-center space-x-2 bg-purple-50 border border-purple-100 px-3 sm:px-4 py-2 rounded-full mb-4 sm:mb-6 max-w-full scroll-reveal" style="--delay: 100ms;">
                        <span class="flex h-2 w-2 shrink-0 rounded-full bg-purple-600"></span>
                        <span
                            class="text-purple-700 text-[10px] sm:text-xs font-bold uppercase tracking-widest leading-tight">Juan
                            XXIII al Servicio de los Pobres</span>
                    </div>

                    <h1
                        class="text-4xl xs:text-5xl sm:text-6xl lg:text-7xl font-black text-gray-900 leading-[1.05] sm:leading-none mb-5 sm:mb-8 scroll-reveal" style="--delay: 200ms;">
                        Alimentando la <br>
                        <span
                            class="text-transparent bg-clip-text bg-gradient-to-r from-purple-600 via-orange-500 to-yellow-500">Esperanza</span>
                    </h1>

                    <p class="text-base sm:text-lg lg:text-xl text-gray-600 mb-6 sm:mb-10 max-w-xl leading-relaxed scroll-reveal" style="--delay: 300ms;">
                        Transformamos vidas a través de la nutrición digna. Conoce nuestros centros de atención y únete
                        a
                        nuestra misión en Nuevo León.
                    </p>

                    <div class="flex flex-row items-center gap-3 sm:gap-4 scroll-reveal" style="--delay: 400ms;">
                        <a href="/comedores"
                            class="bg-purple-600 text-white text-center px-5 sm:px-10 py-4 sm:py-5 rounded-2xl font-bold text-sm sm:text-lg shadow-xl hover:bg-purple-700 transition-all btn-glow flex items-center justify-center">
                            Nuestros Comedores
                        </a>
                        <a href="/donar"
                            class="bg-orange-500 text-white text-center px-5 sm:px-10 py-4 sm:py-5 rounded-2xl font-bold text-sm sm:text-lg shadow-xl hover:bg-orange-600 transition-all btn-glow flex items-center justify-center">
                            Donar Ahora
                        </a>
                    </div>

                    <div class="mt-8 sm:mt-12 flex scroll-reveal" style="--delay: 500ms;">
                        <div
                            class="inline-flex items-center h-10 px-5 rounded-full border-2 border-white bg-yellow-400 text-purple-900 font-bold text-sm shadow-md transition-all duration-300 hover:scale-105 select-none">
                            +50 voluntarios activos hoy
                        </div>
                    </div>
                </div>

                <!-- Imagen / Composición Visual -->
                <div class="lg:col-span-5 relative max-lg:max-w-md max-lg:mx-auto w-full">
                    <div
                        class="relative z-10 rounded-2xl sm:rounded-[2rem] overflow-hidden shadow-2xl rotate-2 sm:rotate-3 hover:rotate-0 transition-transform duration-500 border-4 sm:border-8 border-white scroll-reveal" style="--delay: 300ms;">
                        <img src="/imagenes/fotoportada.webp" alt="Comedor Social"
                            fetchpriority="high"
                            decoding="sync"
                            class="w-full aspect-[4/5] max-h-[min(55vh,420px)] sm:max-h-none object-cover">
                    </div>
                    <!-- Cuadro de acento naranja -->
                    <div class="absolute -bottom-6 -left-6 w-32 h-32 bg-orange-500 rounded-3xl -z-10 -rotate-6"></div>
                    <!-- Cuadro de acento amarillo -->
                    <div class="absolute -top-6 -right-6 w-32 h-32 bg-yellow-400 rounded-3xl -z-10 rotate-12"></div>
                </div>
            </div>
        </section>

        <!-- TRES PILARES (Responsabilidad, Donativos, Participa) -->
        <section class="py-16 md:py-24 bg-gray-50 relative z-20">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 grid md:grid-cols-3 gap-8">
                <!-- Pilar 1 -->
                <div class="bg-white p-10 rounded-[3rem] shadow-xl border border-gray-50 card-hover scroll-reveal" style="--delay: 0ms;">
                    <div
                        class="w-16 h-16 bg-purple-100 rounded-2xl flex items-center justify-center text-purple-600 mb-8 text-2xl">
                        <i class="fas fa-handshake"></i>
                    </div>
                    <h3 class="text-xl font-black text-purple-900 mb-4">Responsabilidad Social</h3>
                    <p class="text-gray-500 text-sm leading-relaxed mb-6">
                        Nuestro compromiso es proveer por lo menos el alimento del día de hoy a las personas
                        que asisten a nuestros comedores, para que puedan enfrentar el día con esperanza.
                    </p>
                    <a href="/nosotros"
                        class="text-purple-600 font-black text-xs uppercase tracking-widest flex items-center group">
                        Leer más <i class="fas fa-chevron-right ml-2 group-hover:ml-4 transition-all"></i>
                    </a>
                </div>

                <!-- Pilar 2 -->
                <div class="bg-white p-10 rounded-[3rem] shadow-xl border border-gray-50 card-hover scroll-reveal" style="--delay: 150ms;">
                    <div
                        class="w-16 h-16 bg-orange-100 rounded-2xl flex items-center justify-center text-orange-600 mb-8 text-2xl">
                        <i class="fas fa-box-open"></i>
                    </div>
                    <h3 class="text-xl font-black text-purple-900 mb-4">Donativos</h3>
                    <p class="text-gray-500 text-sm leading-relaxed mb-6">
                        Tu apoyo puede ser económico o en especie; tu aportación nos ayudará a continuar con la
                        preparación
                        diaria de los alimentos y la operación constante de los Comedores.
                    </p>
                    <a href="/donar"
                        class="text-orange-600 font-black text-xs uppercase tracking-widest flex items-center group">
                        ¿Cómo donar? <i class="fas fa-chevron-right ml-2 group-hover:ml-4 transition-all"></i>
                    </a>
                </div>

                <!-- Pilar 3 -->
                <div class="bg-white p-10 rounded-[3rem] shadow-xl border border-gray-50 card-hover scroll-reveal" style="--delay: 300ms;">
                    <div
                        class="w-16 h-16 bg-yellow-100 rounded-2xl flex items-center justify-center text-yellow-600 mb-8 text-2xl">
                        <i class="fas fa-users"></i>
                    </div>
                    <h3 class="text-xl font-black text-purple-900 mb-4">¡Participa!</h3>
                    <p class="text-gray-500 text-sm leading-relaxed mb-6">
                        Ofrece tu tiempo para ayudar a quien más lo necesita. Apoya en la operación diaria de alimentar
                        a
                        cientos de personas. Cada mano cuenta para transformar realidades.
                    </p>
                    <a href="/contactanos"
                        class="text-yellow-600 font-black text-xs uppercase tracking-widest flex items-center group">
                        Ser Voluntario <i class="fas fa-chevron-right ml-2 group-hover:ml-4 transition-all"></i>
                    </a>
                </div>
            </div>
        </section>

        <!-- SECCIÓN DE NOVEDADES (CAROUSEL DE ANUNCIOS) -->
        <section class="py-12 md:py-16 bg-white">
            <div class="max-w-7xl mx-auto px-4 sm:px-6">
                <!-- Slider Wrapper -->
                <div
                    class="relative overflow-hidden rounded-[3rem] shadow-xl border border-gray-100 bg-white min-h-[600px] xs:min-h-[580px] sm:min-h-[540px] md:min-h-[480px] lg:min-h-[420px] xl:min-h-[380px] scroll-reveal" style="--delay: 100ms;">

                    <!-- SLIDE 1: Carrera -->
                    <div id="slide-carrera"
                        class="announcement-slide slide-3d slide-3d-active p-8 md:p-12 bg-gradient-to-r from-purple-900 to-purple-800 flex items-center">
                        <!-- Decoración de fondo -->
                        <div
                            class="absolute top-0 right-0 w-64 h-64 bg-orange-500 rounded-full mix-blend-multiply filter blur-3xl opacity-10 -mr-20 -mt-20">
                        </div>
                        <div class="relative z-10 grid lg:grid-cols-12 gap-8 lg:gap-12 items-center w-full">
                            <!-- Imagen de la Carrera -->
                            <div class="lg:col-span-5 order-2 lg:order-1 flex justify-center">
                                <img src="/imagenes/correyalimenta.webp" loading="lazy"
                                    class="w-full max-h-[160px] sm:max-h-[200px] lg:max-h-[250px] object-cover rounded-[2rem] shadow-xl -rotate-1 hover:rotate-0 transition duration-500"
                                    alt="Carrera">
                            </div>
                            <!-- Texto Principal -->
                            <div class="lg:col-span-7 order-1 lg:order-2 space-y-4 text-center lg:text-left">
                                <div
                                    class="inline-flex items-center gap-2 px-3 py-1 bg-white/10 text-orange-400 rounded-full text-[10px] font-black uppercase tracking-widest mb-1 border border-white/10">
                                    <i class="fas fa-running"></i> Evento con Causa
                                </div>
                                <h2 class="text-3xl lg:text-4xl font-black text-white mb-2 leading-tight">
                                    Corre y Alimenta la <span class="text-orange-500">Esperanza</span>
                                </h2>
                                <p class="text-purple-100 text-sm md:text-base leading-relaxed opacity-90">
                                    Participa en nuestra carrera anual. Todo lo recaudado se destina directamente al
                                    sostenimiento de nuestros comedores y la compra de alimentos para quienes más lo
                                    necesitan.
                                </p>
                                <div class="pt-2">
                                    <a href="https://www.instagram.com/correyalimentalaesperanza?igsh=YTdmcXRzMGdjMmJz"
                                        target="_blank"
                                        class="inline-flex items-center px-8 py-4 bg-orange-500 text-white rounded-full font-black text-xs hover:bg-orange-600 transition shadow-lg shadow-orange-950/50 gap-3 group uppercase tracking-widest active:scale-95 duration-150 cursor-pointer">
                                        <span>Ver en Instagram</span>
                                        <i class="fab fa-instagram text-base group-hover:scale-110 transition"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- SLIDE 2: Informe -->
                    <div id="slide-informe"
                        class="announcement-slide slide-3d slide-3d-right p-8 md:p-12 bg-gray-50 flex items-center">
                        <div class="relative z-10 grid lg:grid-cols-12 gap-8 lg:gap-12 items-center w-full">
                            <div class="lg:col-span-5 order-2 lg:order-1 flex justify-center">
                                <img src="/imagenes/informeactividades.webp" loading="lazy" fetchpriority="low"
                                    class="w-full max-h-[160px] sm:max-h-[200px] lg:max-h-[250px] object-cover rounded-[2rem] shadow-xl rotate-1 hover:rotate-0 transition duration-500"
                                    alt="Reporte">
                            </div>
                            <div class="lg:col-span-7 order-1 lg:order-2 space-y-4 text-center lg:text-left">
                                <div
                                    class="inline-flex items-center gap-2 px-3 py-1 bg-purple-50 text-purple-600 rounded-full text-[10px] font-black uppercase tracking-widest mb-1 border border-purple-100">
                                    <i class="fas fa-file-alt"></i> Transparencia
                                </div>
                                <h3 class="text-3xl lg:text-4xl font-black text-purple-900 leading-tight">Informe Anual 2025</h3>
                                <p class="text-[10px] text-gray-400 font-bold uppercase tracking-wider">Periodo Enero -
                                    Diciembre 2025</p>
                                <p
                                    class="text-gray-500 text-sm md:text-base leading-relaxed opacity-90 max-w-xl mx-auto lg:mx-0">
                                    La transparencia es el pilar de nuestra confianza. Consulta nuestros logros y el
                                    impacto directo que hemos generado gracias a tu apoyo constante.
                                </p>
                                <div class="pt-2">
                                    <a href="/informe/Informe 2025.pdf" download="Informe-Anual-2025.pdf"
                                        class="inline-flex items-center px-8 py-4 bg-purple-600 text-white font-black rounded-2xl hover:bg-purple-700 transition shadow-lg shadow-purple-200 mx-auto lg:mx-0 text-xs active:scale-95 duration-150 cursor-pointer">
                                        <i class="fas fa-file-pdf mr-3"></i> DESCARGAR REPORTE PDF
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- CAROUSEL CONTROLS -->
                    <!-- Left Arrow -->
                    <button onclick="prevSlide()"
                        class="absolute left-4 top-1/2 -translate-y-1/2 w-10 h-10 rounded-full bg-white/20 backdrop-blur-sm border border-white/30 text-white hover:bg-white/40 flex items-center justify-center focus:outline-none transition z-20 cursor-pointer shadow-md"
                        id="carousel-prev">
                        <i class="fas fa-chevron-left text-sm"></i>
                    </button>
                    <!-- Right Arrow -->
                    <button onclick="nextSlide()"
                        class="absolute right-4 top-1/2 -translate-y-1/2 w-10 h-10 rounded-full bg-white/20 backdrop-blur-sm border border-white/30 text-white hover:bg-white/40 flex items-center justify-center focus:outline-none transition z-20 cursor-pointer shadow-md"
                        id="carousel-next">
                        <i class="fas fa-chevron-right text-sm"></i>
                    </button>

                </div>
            </div>
        </section>

        <!-- SCRIPT DEL CAROUSEL DE ANUNCIOS -->
        <script>
            // ----- Carousel State -----
            /** @type {number} */
            let currentSlideIndex = 0;
            /** @type {number | undefined} */
            let autoRotateInterval;
            /** @type {boolean} */
            let isCarouselInitialized = false;

            /**
             * ----- Show Slide Logic -----
             * @param {number} index
             * @param {'next'|'prev'} direction
             */
            function showSlide(index, direction = 'next') {
                const slides = document.querySelectorAll('.announcement-slide');
                const prevBtn = document.getElementById('carousel-prev');
                const nextBtn = document.getElementById('carousel-next');

                if (!slides.length) return;

                if (!isCarouselInitialized) {
                    isCarouselInitialized = true;
                    currentSlideIndex = 0;

                    // ----- Initialize Controls -----
                    prevBtn.className = "absolute left-4 top-1/2 -translate-y-1/2 w-10 h-10 rounded-full bg-white/20 backdrop-blur-sm border border-white/30 text-white hover:bg-white/40 flex items-center justify-center focus:outline-none transition z-20 cursor-pointer shadow-md opacity-0 pointer-events-none";
                    nextBtn.className = "absolute right-4 top-1/2 -translate-y-1/2 w-10 h-10 rounded-full bg-white/20 backdrop-blur-sm border border-white/30 text-white hover:bg-white/40 flex items-center justify-center focus:outline-none transition z-20 cursor-pointer shadow-md";
                    return;
                }

                const outgoingIndex = currentSlideIndex;
                currentSlideIndex = index;

                if (outgoingIndex === index) {
                    return;
                }

                const outgoingSlide = slides[outgoingIndex];
                const incomingSlide = slides[index];

                // ----- Reset Transition Classes -----
                outgoingSlide.classList.remove('slide-3d-active', 'slide-3d-left', 'slide-3d-right');
                incomingSlide.classList.remove('slide-3d-active', 'slide-3d-left', 'slide-3d-right');

                if (direction === 'next') {
                    // ----- Next Transition -----
                    outgoingSlide.classList.add('slide-3d-left');
                    incomingSlide.classList.add('slide-3d-right');
                    void incomingSlide.offsetWidth;
                    incomingSlide.classList.remove('slide-3d-right');
                    incomingSlide.classList.add('slide-3d-active');
                } else {
                    // ----- Prev Transition -----
                    outgoingSlide.classList.add('slide-3d-right');
                    incomingSlide.classList.add('slide-3d-left');
                    void incomingSlide.offsetWidth;
                    incomingSlide.classList.remove('slide-3d-left');
                    incomingSlide.classList.add('slide-3d-active');
                }

                // ----- Update Arrow Visibility -----
                const isDarkBackground = index === 0;

                if (isDarkBackground) {
                    prevBtn.className = "absolute left-4 top-1/2 -translate-y-1/2 w-10 h-10 rounded-full bg-white/20 backdrop-blur-sm border border-white/30 text-white hover:bg-white/40 flex items-center justify-center focus:outline-none transition z-20 cursor-pointer shadow-md opacity-0 pointer-events-none";
                    nextBtn.className = "absolute right-4 top-1/2 -translate-y-1/2 w-10 h-10 rounded-full bg-white/20 backdrop-blur-sm border border-white/30 text-white hover:bg-white/40 flex items-center justify-center focus:outline-none transition z-20 cursor-pointer shadow-md";
                } else {
                    prevBtn.className = "absolute left-4 top-1/2 -translate-y-1/2 w-10 h-10 rounded-full bg-purple-100 hover:bg-purple-200 border border-purple-200/50 text-purple-700 flex items-center justify-center focus:outline-none transition z-20 cursor-pointer shadow-md";
                    nextBtn.className = "absolute right-4 top-1/2 -translate-y-1/2 w-10 h-10 rounded-full bg-purple-100 hover:bg-purple-200 border border-purple-200/50 text-purple-700 flex items-center justify-center focus:outline-none transition z-20 cursor-pointer shadow-md opacity-0 pointer-events-none";
                }
            }

            function nextSlide() {
                const slides = document.querySelectorAll('.announcement-slide');
                if (!slides.length) return;
                const nextIndex = (currentSlideIndex + 1) % slides.length;
                showSlide(nextIndex, 'next');
                resetAutoRotate();
            }

            function prevSlide() {
                const slides = document.querySelectorAll('.announcement-slide');
                if (!slides.length) return;
                const prevIndex = (currentSlideIndex - 1 + slides.length) % slides.length;
                showSlide(prevIndex, 'prev');
                resetAutoRotate();
            }

            function startAutoRotate() {
                clearInterval(autoRotateInterval);
                autoRotateInterval = setInterval(() => {
                    const slides = document.querySelectorAll('.announcement-slide');
                    if (!slides.length) return;
                    const nextIndex = (currentSlideIndex + 1) % slides.length;
                    const direction = nextIndex === 0 ? 'prev' : 'next';
                    showSlide(nextIndex, direction);
                }, 5000);
            }

            function resetAutoRotate() {
                clearInterval(autoRotateInterval);
                startAutoRotate();
            }

            // ----- Initialization -----
            document.addEventListener('DOMContentLoaded', () => {
                showSlide(0);
                startAutoRotate();

                // ----- Hover Pause Logic -----
                const slider = document.getElementById('slide-carrera');
                if (slider && slider.parentElement) {
                    const container = slider.parentElement;
                    container.addEventListener('mouseenter', () => clearInterval(autoRotateInterval));
                    container.addEventListener('mouseleave', startAutoRotate);
                }
            });
        </script>

        <!-- SECCIÓN DE CAMPAÑAS Y VIDEOS -->
        <section class="py-16 md:py-24 bg-gray-50">
            <div class="max-w-7xl mx-auto px-4 sm:px-6">
                <div class="text-center mb-16">
                    <h2 class="text-4xl font-black text-purple-900 italic">Acciones que Dejan Huella</h2>
                    <div class="w-24 h-1.5 bg-yellow-400 mx-auto mt-4 rounded-full"></div>
                </div>

                <div class="grid md:grid-cols-3 gap-10">
                    <!-- Video/Campaña 1 -->
                    <a href="https://www.youtube.com/watch?v=bkDXa2lRu9s" target="_blank"
                        class="block group cursor-pointer scroll-reveal" style="--delay: 0ms;">
                        <div class="relative rounded-[2rem] overflow-hidden aspect-video shadow-lg mb-6">
                            <img src="/imagenes/video-institucional.webp" loading="lazy"
                                class="w-full h-full object-cover group-hover:scale-105 transition-all duration-500">
                            <div class="absolute inset-0 bg-black/40 flex items-center justify-center">
                                <div
                                    class="w-14 h-14 bg-white/20 backdrop-blur-md rounded-full flex items-center justify-center text-white border border-white/30 group-hover:scale-110 transition">
                                    <i class="fas fa-play text-xl ml-1"></i>
                                </div>
                            </div>
                        </div>
                        <p class="text-sm font-bold text-purple-900 uppercase tracking-widest text-center">Video
                            Institucional</p>
                    </a>

                    <!-- Video/Campaña 2 -->
                    <a href="https://www.youtube.com/watch?v=GY7a6vkFO3s" target="_blank"
                        class="block group cursor-pointer scroll-reveal" style="--delay: 150ms;">
                        <div class="relative rounded-[2rem] overflow-hidden aspect-video shadow-lg mb-6">
                            <img src="/imagenes/nuestra-directora.webp" loading="lazy"
                                class="w-full h-full object-cover group-hover:scale-105 transition-all duration-500">
                            <div class="absolute inset-0 bg-black/40 flex items-center justify-center">
                                <div
                                    class="w-14 h-14 bg-white/20 backdrop-blur-md rounded-full flex items-center justify-center text-white border border-white/30 group-hover:scale-110 transition">
                                    <i class="fas fa-play text-xl ml-1"></i>
                                </div>
                            </div>
                        </div>
                        <p class="text-sm font-bold text-purple-900 uppercase tracking-widest text-center">Nuestra
                            Directora
                        </p>
                    </a>

                    <!-- Video/Campaña 3 -->
                    <a href="https://www.youtube.com/watch?v=hJ_-4FF7Xr0" target="_blank"
                        class="block group cursor-pointer scroll-reveal" style="--delay: 300ms;">
                        <div class="relative rounded-[2rem] overflow-hidden aspect-video shadow-lg mb-6">
                            <img src="/imagenes/comedor-unidad-escobedo.webp" loading="lazy"
                                class="w-full h-full object-cover group-hover:scale-105 transition-all duration-500">
                            <div class="absolute inset-0 bg-black/40 flex items-center justify-center">
                                <div
                                    class="w-14 h-14 bg-white/20 backdrop-blur-md rounded-full flex items-center justify-center text-white border border-white/30 group-hover:scale-110 transition">
                                    <i class="fas fa-play text-xl ml-1"></i>
                                </div>
                            </div>
                        </div>
                        <p class="text-sm font-bold text-purple-900 uppercase tracking-widest text-center">Comedor
                            Unidad
                            Escobedo</p>
                    </a>

                </div>
            </div>
        </section>

        <!-- FOOTER -->
        <%- include('partials/footer') %>
</body>

</html>